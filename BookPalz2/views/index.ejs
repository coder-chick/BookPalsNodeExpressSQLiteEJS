<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book Pals Application</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <div class="container">
      <nav></nav>

      <main class="row">
        <h1>Book Pals application</h1>
        <div class="col-8">
          <div>
            A book swapping management system that allows book owners loan their favorite books to borrowers residing in the same geographical region.

          </div>

          <h2>Borrowed Books</h2>
          <ul>
            <% book.forEach(function (f) { %>

            <li><a href="/book/<%= f.isbn %>"><%= f.book_title %> 👉 <%= f.year_of_publication %></a>
              <form action="/book/delete" method="POST">
                <input type="hidden" name="isbn" value="<%= f.isbn %>">
                <button type="submit" class="btn btn-danger">Delete</button>
              </form>
            </li>

            <% })%>
          </ul>
        </div>
        <!-- /.col-8 -->

        <div class="col-4">
          <h2>Create Book</h2>

          <form action="/book/create" method="POST">
            <div>
              <label class="form-label"
                >Book Title:
                <input
                  required="true"
                  name="book_title"
                  type="text"
                  class="form-control"
              /></label>
            </div>
            <div>
              <label class="form-label"
                >Year of Publication:
                <input
                  required="true"
                  name="year_of_publication"
                  type="text"
                  class="form-control"
                  value="1950"
              /></label>
            </div>
            <div>
              <label class="form-label"
                >Author ID:
                <input
                  required="true"
                  name="author_id"
                  type="integer"
                  class="form-control"
                  value="1"
                  
              /></label>
            </div>
            <div>
              <label class="form-label"
                >User ID:
                <input
                  required="true"
                  value="10"
                  name="user_id"
                  type="integer"
                  class="form-control"
              /></label>
            </div>

            <div>
              <label class="form-label"
                >Region ID:
                <input
                  required="true"
                  value="10"
                  name="region_id"
                  type="number"
                  class="form-control"
              /></label>
            </div>

            <button type="submit" class="btn btn-primary">Create</button>
          </form>
        </div>
        <!-- /.col-4 -->
      </main>

      <footer>
        <div class="row">
          <div class="col-4">Northeastern University CS 5200</div>
          <div class="col-4">Made By Yvette and Semaa with blood, sweat, tears and precious ⏰</div>
          <div class="col-4">Copyright©</div>
        </div>
      </footer>
    </div>
  </body>
</html>
